server:
  port: 8088

spring:
  application:
    name: lab05
  datasource:
      url: jdbc:mysql://localhost:3306/lab05?useUnicode=true&characterEncoding=utf-8&useSSL=false
      username: root
      password: root
      driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    generate-ddl: true
    show-sql: true
